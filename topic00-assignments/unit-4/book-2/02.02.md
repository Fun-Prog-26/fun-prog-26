# Parsing From XML - Example

This example is a minimum working example where you take data in from a xml file using xml-conduit package and generates  (possibly a formatted version of) a  markdown file. 
The stack package is [here](archives/bookxml.zip).


You should look at the code, play around with the reading into the ADT and the  formatting of markdown, see what happens when you change the formatting.


